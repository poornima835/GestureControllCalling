# GestureControllCalling
Its an android based calling application by using which we can call to a particular number just on double click . I had given the number  during the time of code. Also it will  
